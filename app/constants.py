"""
Константы приложения MindType.

Централизованное хранение всех магических чисел и настроек.
"""

# =============================================================================
# Audio
# =============================================================================

AUDIO_SAMPLERATE = 16000
AUDIO_CHANNELS = 1
AUDIO_DTYPE = "int16"
AUDIO_BLOCK_SIZE = 1024
RMS_NORMALIZER = 2000

# =============================================================================
# Assistant VAD (Voice Activity Detection)
# =============================================================================

VAD_CALIBRATION_DURATION = 0.3  # Длительность калибровки (секунды)
VAD_SILENCE_DURATION = 1.2      # Длительность тишины для завершения записи (секунды)
VAD_MIN_SPEECH_DURATION = 0.3   # Минимальная длительность речи (секунды)

# Множители для динамического расчёта порогов на основе шума окружения
VAD_NOISE_MULTIPLIER_SILENCE = 1.3  # Множитель для порога тишины
VAD_NOISE_MULTIPLIER_SPEECH = 2.0   # Множитель для порога речи

# Границы порога речи
VAD_MIN_SPEECH_THRESHOLD = 0.06     # Минимальный порог (для тихих помещений)
VAD_MAX_SPEECH_THRESHOLD = 0.25     # Максимальный порог (для шумных помещений)

# Таймауты
VAD_WAITING_TIMEOUT_LOOPS = 50      # 5 секунд (50 * 0.1s)
VAD_MAX_RECORDING_TIME = 30.0       # Максимальное время записи (секунды)

# =============================================================================
# Voice Interrupt
# =============================================================================

VOICE_INTERRUPT_THRESHOLD = 0.10    # RMS порог для прерывания голосом
VOICE_INTERRUPT_MIN_HITS = 3        # Минимум последовательных срабатываний

# =============================================================================
# UI
# =============================================================================

HISTORY_MAX_ENTRIES = 20            # Максимум записей в истории диалогов
WAVEFORM_BARS = 32                  # Количество полос в визуализации волны

# =============================================================================
# Crash Reporter
# =============================================================================

MAX_BREADCRUMBS = 50                # Максимум хранимых breadcrumbs

# =============================================================================
# Config
# =============================================================================

# Поля с API ключами (для keyring)
API_KEY_FIELDS = (
    "openai_api_key",
    "anthropic_api_key",
    "gemini_api_key",
    "openrouter_api_key",
)
