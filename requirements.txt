# Основные зависимости
PyQt6>=6.11.0
sounddevice>=0.5.5
numpy>=2.4.4
huggingface-hub>=1.12.0
pyperclip>=1.8.2

# ONNX runtime с поддержкой NPU и GPU
onnxruntime-openvino>=1.17.0; sys_platform == 'win32'
onnxruntime>=1.17.0; sys_platform != 'win32'
optimum[onnxruntime]>=2.1.0

# Transformers для Whisper ONNX
transformers>=5.6.2
librosa>=0.10.0
soundfile>=0.12.0

# Опционально: legacy faster-whisper
# faster-whisper>=1.0.3
# ctranslate2==4.4.0

# Кросс-платформенные хоткеи и ввод
pynput>=1.8.2

# Windows-специфичные
keyboard>=0.13.5; sys_platform == 'win32'

# macOS-специфичные
pyobjc-framework-Cocoa>=12.1; sys_platform == 'darwin'
pyobjc-framework-Quartz>=9.0; sys_platform == 'darwin'

# Системные и прочее
python-dotenv>=1.0.0
scikit-learn>=1.0.0
psutil>=7.2.2
keyring>=24.0.0

# Тестирование (dev)
pytest>=9.0.3
pytest-cov>=4.0.0

# Голосовой ассистент
openwakeword>=0.6.0      # Wake word detection
edge-tts>=6.1.0          # Microsoft TTS
pydub>=0.25.0            # Декодирование MP3 для TTS (опционально, fallback на ffmpeg)
num2words>=0.5.13        # Числа в слова
dateparser>=1.4.0        # Парсинг дат
transliterate>=1.10.2    # Транслитерация
# tflite-runtime - опционально, openWakeWord может работать с ONNX
