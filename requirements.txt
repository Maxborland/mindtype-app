# Основные зависимости
PyQt6>=6.6.0
sounddevice>=0.4.6
numpy>=1.26
huggingface-hub>=0.20.0
pyperclip>=1.8.2

# ONNX runtime для VAD (DirectML на Windows)
onnxruntime-directml>=1.17.0; sys_platform == 'win32'
onnxruntime>=1.17.0; sys_platform != 'win32'

# Опционально: legacy faster-whisper (если нужен fallback)
# faster-whisper>=1.0.3
# ctranslate2==4.4.0

# Кросс-платформенные хоткеи и ввод (для macOS и Linux)
pynput>=1.7.6

# Windows-специфичные (только для Windows, keyboard использует Windows API напрямую)
keyboard>=0.13.5; sys_platform == 'win32'

# macOS-специфичные (только для macOS)
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'
pyobjc-framework-Quartz>=9.0; sys_platform == 'darwin'

# Для сборки (выберите один из вариантов)
# PyInstaller - простая сборка

# Nuitka - компиляция в нативный код (защита от декомпиляции)
# Рекомендуется для production сборок
nuitka>=2.8.0
ordered-set>=4.1.0
zstandard>=0.22.0

# Опционально: для загрузки конфигурации из .env файла
python-dotenv>=1.0.0

# LLM для локальной суммаризации (Qwen3)
llama-cpp-python>=0.3.0

# Постобработка транскрипций
# Восстановление пунктуации (rule-based fallback)
# deepmultilingualpunctuation удален для уменьшения размера (torch ~4GB)

# Аудио обработка
librosa>=0.10.0
soundfile>=0.12.0
scikit-learn>=1.0.0

# Системные
psutil>=5.9.0
